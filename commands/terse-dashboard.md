---
description: Regenerate and open the local Terse savings dashboard in your browser.
---

Call the `terse_dashboard` MCP tool. It regenerates `~/.terse/dashboard.html` from
the metering data and opens it in the default browser. Report the file path and the
headline savings figure to the user.
