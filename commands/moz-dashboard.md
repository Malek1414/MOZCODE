---
description: Regenerate and open the local MOZCODE savings dashboard in your browser.
---

Call the `moz_dashboard` MCP tool. It regenerates `~/.mozcode/dashboard.html` from
the metering data and opens it in the default browser. Report the file path and the
headline savings figure to the user.
