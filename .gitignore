node_modules/
*.log
.DS_Store

# dist/ (bundled server) and grammars/*.wasm ARE committed so the plugin is
# installable with no build step. Do not ignore them.

# Local research / benchmark docs — kept out of the public repo
wozcode-teardown.md
benchmark.md
